export default {
  countries(state) {
    return state.list;
  },
  lastAdded(state) {
    return state.lastAdded;
  },
  lastRemoved(state) {
    return state.lastRemoved;
  },
  hidden(state) {
    return state.hidden;
  },
};
